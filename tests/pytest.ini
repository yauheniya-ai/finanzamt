[pytest]
testpaths = src/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --tb=short
    -q
    --strict-markers
markers =
    slow: marks tests as slow (skip with -m "not slow")
    integration: marks tests that require external services (Ollama, Tesseract)
